# Submission checklist — due Monday, August 24

Send to **byron.dover@modernclassrooms.org**.

## The four deliverables

1. **The video** — unlisted YouTube link (openable without requesting
   access). Upload `out/volume_cubes_vN.mp4`; attach
   `out/volume_cubes_vN.srt` as the caption track (YouTube Studio →
   Subtitles → Upload file → With timing). Title suggestion:
   *"Volume with Whole-Number Cubes — Grade 6 Guided Notes (Lesson 1,
   Skill 1)"*. Confirm the link works in a private window.
2. **The repo** — flip `tsappington/rostrum` to public (Settings →
   Danger Zone → Change visibility) at send time, not before.
   Pre-flip check: README finalized, LICENSE present, no stray files in
   `out/` tracked, history clean.
3. **README.md** — promote the finalized draft (Ted's voice) from
   `docs/readme_draft.md` to the repo root, replacing the stub.
4. **Optional screencast (≤5 min)** — worth doing: a filmmaker walking
   through his own pipeline is free evidence of on-camera communication.
   Suggested shape (3–4 min): the film's opening 30s → the capture tool
   with a live stroke → the variant strip ("no two 12s alike") → the
   timing map schedule printout → one sentence on where production goes.

## Email draft (Ted's voice — edit freely)

> Subject: Performance task — Ted Sappington
>
> Hi Byron,
>
> Here's my submission for the AI Video Generation Architect
> performance task:
>
> - **Video** (3:53): [unlisted YouTube link]
> - **Repo**: https://github.com/tsappington/rostrum
> - **README**: in the repo root — approach, stack, trade-offs, and
>   where I'd take it next. It's written to anchor a conversation;
>   I'm looking forward to that.
>
> One production note I'll flag up front: the teacher's handwriting is
> literally mine — captured on an iPad with a tool built for this
> project, replayed with its real timing, and drawn from a library of
> takes so no two instances of a number are identical.
>
> Thanks for a genuinely fun brief.
>
> Ted

## Before hitting send

- [ ] Watch the final cut start to finish, sound on, full screen
- [ ] Partner screening (the 6th-grade proxy) — note anything she flags
- [ ] Math re-verified: `python -m rostrum.verify` prints all-pass
      (it also runs first on every `tools.film` entry point, so a bad
      number can't reach a render)
- [ ] Captions spot-checked against audio at 2–3 points
- [ ] README final read in the browser (GitHub rendering)
- [ ] Repo public, video link tested logged-out
